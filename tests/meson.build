# SPDX-License-Identifier: GPL-3.0-or-later
#
# Stress and benchmark targets, gated by `-Dstress=true`. Tests link against
# framework_lib (the static library declared in src/meson.build) so internal
# symbols (fw_cache_*, fw_document_*) are reachable without exposing them
# as a public API.

subdir('stress')
