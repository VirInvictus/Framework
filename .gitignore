# SPDX-License-Identifier: GPL-3.0-or-later

# Build directories
build/
builddir/

# Meson generated files
subprojects/

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# LSP / clangd (per-developer)
/.clangd
/compile_commands.json

# Compiled objects
*.o
*.so
*.a

# Spec (internal planning doc)
framework-spec.md

# Claude Code
claude.run

# Vendored reference repos for Phase 13.1 reflow rewrite
# (see CLAUDE.md "Reference repos" and docs/foliate-rewrite.md). The
# other seven repos (zathura, zathura-mupdf, sumatrapdf, sioyek,
# plato, yacreader, mcomix) were removed in v0.39.0 after their
# patterns were extracted — see roadmap.md and patchnotes.md v0.39.0.
.foliate/
.foliate-js/
.komikku/

# Local test corpus — copyrighted sample documents, never committed.
# The stress/bench suite resolves this directory by default (override
# with FW_TEST_CORPUS_ROOT). See tests/corpus.json for the expected
# filenames and tests/README.md for how to populate it.
.testfiles/

# OS junk
.DS_Store
Thumbs.db
